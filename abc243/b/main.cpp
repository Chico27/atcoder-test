#include<bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;

int main()
{
  int s, a, b=0;
  cin >> s;
  cin >> a >> b;

  string s;
  cin >> s;
  cout << s << endl;
}
